window.myModal = (typeof window.myModal === 'undefined') ? new bootstrap.Modal(document.getElementById('newEvent')) : window.myModal
document.getElementById('newEventLabel').innerHTML = "New Recurring Event"
document.getElementsByClassName('modal-body')[0].innerHTML = '<%= j render "form", action: "/recurring_events" %>'
window.myModal.show()
